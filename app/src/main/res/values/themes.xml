<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FitConnect" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FitConnect" parent="Base.Theme.FitConnect" />

    <!-- Tema Splash: fundo escuro imediato para evitar flash branco -->
    <style name="Theme.FitConnect.Splash" parent="Theme.FitConnect">
        <item name="android:windowBackground">@color/fit_background</item>
        <item name="android:statusBarColor">@color/fit_background</item>
        <item name="android:navigationBarColor">@color/fit_background</item>
    </style>

    <!-- Tema Transparente para o Menu Lateral -->
    <style name="Theme.FitConnect.Transparent" parent="Theme.FitConnect">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>